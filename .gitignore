# Environments
.venv
__pycache__/
*.pyc

# Local tooling
.claude/

# Secrets
.env
# Local compose holds real API keys; docker-compose.example.yml is the template.
docker-compose.yml

# Local user configuration (use config/keywords.example.yaml as a template)
config/keywords.yaml

# Runtime outputs (generated on each run)
vagas_filtradas.md
vagas_historico.json
vagas_historico.backup-*.json
data/benchmark_*.md
*.xlsx
