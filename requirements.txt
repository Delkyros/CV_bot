requests>=2.31.0
beautifulsoup4>=4.12.0
pyyaml>=6.0.1
python-dotenv>=1.0.1
google-genai>=0.1.1
# Web UI to mark jobs as viewed/applied (webapp.py)
flask>=3.0,<4
# Local, offline match scoring — fallback when every LLM provider fails (src/local_match.py)
chonkie[model2vec]>=1.6,<2
